server.port=8080

spring.application.name=praxis-backend

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:postgres}?currentSchema=${DB_SCHEMA:public}
spring.datasource.username=${DB_USER:${PRAXIS_DATASOURCE_USERNAME:postgres}}
spring.datasource.password=${DB_PASS:${PRAXIS_DATASOURCE_PASSWORD:postgres}}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.default-schema=${DB_SCHEMA:public}
spring.flyway.schemas=${DB_SCHEMA:public}

spring.jpa.hibernate.ddl-auto=${PRAXIS_JPA_DDL_AUTO:none}
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.default_schema=${DB_SCHEMA:public}

spring.datasource.hikari.minimum-idle=${SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE:1}
spring.datasource.hikari.maximum-pool-size=${SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE:5}

server.tomcat.threads.min-spare=${SERVER_TOMCAT_THREADS_MIN_SPARE:5}
server.tomcat.threads.max=${SERVER_TOMCAT_THREADS_MAX:50}

spring.jmx.enabled=${SPRING_JMX_ENABLED:false}
spring.main.lazy-initialization=${SPRING_MAIN_LAZY_INITIALIZATION:false}

praxis.public-base-url=${PRAXIS_PUBLIC_BASE_URL:http://localhost:8080}
praxis.candidate-page-base-url=${PRAXIS_CANDIDATE_PAGE_BASE_URL:${PRAXIS_PUBLIC_BASE_URL:http://localhost:8080}}
praxis.attempt-link-ttl-hours=${PRAXIS_ATTEMPT_LINK_TTL_HOURS:168}
praxis.attempt-session-ttl-hours=${PRAXIS_ATTEMPT_SESSION_TTL_HOURS:24}
praxis.candidate-result-ttl-hours=${PRAXIS_CANDIDATE_RESULT_TTL_HOURS:720}
praxis.recommend-interview-threshold=${PRAXIS_RECOMMEND_INTERVIEW_THRESHOLD:70}
praxis.answer-grace-period-seconds=${PRAXIS_ANSWER_GRACE_PERIOD_SECONDS:15}
praxis.competency-weight-tolerance=${PRAXIS_COMPETENCY_WEIGHT_TOLERANCE:0.001}
praxis.security.enabled=${PRAXIS_SECURITY_ENABLED:true}
# PRAXIS_DEFAULT_TENANT_ID e aceito como fallback legado da variavel renomeada.
praxis.default-empresa-id=${PRAXIS_DEFAULT_EMPRESA_ID:${PRAXIS_DEFAULT_TENANT_ID:empresa-1}}
praxis.jwt-secret=${PRAXIS_JWT_SECRET}
praxis.jwt-expiration-hours=${PRAXIS_JWT_EXPIRATION_HOURS:8}
praxis.cors.allowed-origins=${PRAXIS_CORS_ALLOWED_ORIGINS:}

# Recuperação de senha. O link aponta para PRAXIS_PUBLIC_BASE_URL + /reset-password/{token}.
# O token expira automaticamente (TTL padrao de 2 horas) e nunca e armazenado em texto puro.
praxis.auth.password-reset-ttl-hours=${PRAXIS_AUTH_PASSWORD_RESET_TTL_HOURS:2}
praxis.auth.password-reset-max-attempts=${PRAXIS_AUTH_PASSWORD_RESET_MAX_ATTEMPTS:5}
praxis.auth.password-reset-window-minutes=${PRAXIS_AUTH_PASSWORD_RESET_WINDOW_MINUTES:15}
# Em dev/homologacao pode-se logar o link de redefinicao. Mantenha false em producao.
praxis.auth.password-reset-log-link=${PRAXIS_AUTH_PASSWORD_RESET_LOG_LINK:false}

# Mercado Pago (Parte B). Credenciais ficam apenas no backend, lidas de variaveis de
# ambiente e nunca commitadas no repositorio. Mantenha mp.enabled=false ate configurar.
mp.enabled=${MP_ENABLED:false}
mp.base-url=${MP_BASE_URL:https://api.mercadopago.com}
mp.access-token=${MP_ACCESS_TOKEN:}
mp.public-key=${MP_PUBLIC_KEY:}
mp.webhook-secret=${MP_WEBHOOK_SECRET:}
mp.grace-period-days=${MP_GRACE_PERIOD_DAYS:7}
mp.back-url=${MP_BACK_URL:${PRAXIS_PUBLIC_BASE_URL:http://localhost:8080}}
mp.notification-url=${MP_NOTIFICATION_URL:}
mp.delinquency-cron=${MP_DELINQUENCY_CRON:0 0 * * * *}
praxis.privacy-retention-days=${PRAXIS_PRIVACY_RETENTION_DAYS:180}
praxis.privacy-retention-enabled=${PRAXIS_PRIVACY_RETENTION_ENABLED:true}
praxis.privacy-retention-cron=${PRAXIS_PRIVACY_RETENTION_CRON:0 30 3 * * *}

# Retencao proativa: painel de Saude do Cliente (Health Score) e fila de atuacao de Customer Success.
# A varredura diaria compara as conclusoes dos ultimos N dias com o periodo anterior de mesmo tamanho
# e alerta o CS sobre clientes ativos cuja queda supera o limite (por padrao, mais de 30%).
praxis.retention.customer-health-enabled=${PRAXIS_RETENTION_CUSTOMER_HEALTH_ENABLED:true}
praxis.retention.customer-health-cron=${PRAXIS_RETENTION_CUSTOMER_HEALTH_CRON:0 0 8 * * *}
praxis.retention.health-period-days=${PRAXIS_RETENTION_HEALTH_PERIOD_DAYS:30}
praxis.retention.at-risk-drop-threshold=${PRAXIS_RETENTION_AT_RISK_DROP_THRESHOLD:0.30}
praxis.retention.min-baseline-completions=${PRAXIS_RETENTION_MIN_BASELINE_COMPLETIONS:3}
praxis.retention.cs-team-email=${PRAXIS_RETENTION_CS_TEAM_EMAIL:}

# Relatorios de engajamento automatizados. O periodo e expresso em dias.
praxis.engagement.report-enabled=${PRAXIS_ENGAGEMENT_REPORT_ENABLED:true}
praxis.engagement.report-cron=${PRAXIS_ENGAGEMENT_REPORT_CRON:0 0 7 1 * *}
praxis.engagement.report-period-days=${PRAXIS_ENGAGEMENT_REPORT_PERIOD_DAYS:30}
# Controla apenas a estimativa de tempo poupado; false mantem o relatorio sem apresentar a estimativa.
praxis.engagement.time-saving-estimate-enabled=${PRAXIS_ENGAGEMENT_TIME_SAVING_ESTIMATE_ENABLED:true}
# Hipotese em horas por avaliacao concluida. Nao representa economia observada sem estudo comparativo.
praxis.engagement.hours-saved-per-evaluation=${PRAXIS_ENGAGEMENT_HOURS_SAVED_PER_EVALUATION:1.5}
# Identifica a origem da hipotese exibida no relatorio. Substitua pelo estudo ou criterio interno adotado.
praxis.engagement.time-saving-methodology-source=${PRAXIS_ENGAGEMENT_TIME_SAVING_METHODOLOGY_SOURCE:Hipotese operacional configurada pela organizacao}

# Regua de cobranca inteligente (dunning): toques educativos de retry quando o pagamento falha e
# lembretes recorrentes enquanto o cliente segue PENDENTE_PAGAMENTO/INADIMPLENTE, antes da suspensao.
praxis.billing.dunning-enabled=${PRAXIS_BILLING_DUNNING_ENABLED:true}
praxis.billing.dunning-reminder-cron=${PRAXIS_BILLING_DUNNING_REMINDER_CRON:0 0 9 * * *}
praxis.billing.dunning-reminder-min-interval-hours=${PRAXIS_BILLING_DUNNING_REMINDER_MIN_INTERVAL_HOURS:20}
praxis.privacy.controller-name=${PRAXIS_PRIVACY_CONTROLLER_NAME:Controlador a ser informado pelo empresa}
praxis.privacy.service-email=${PRAXIS_PRIVACY_SERVICE_EMAIL:}
praxis.privacy.service-url=${PRAXIS_PRIVACY_SERVICE_URL:}
praxis.privacy.dpo-contact=${PRAXIS_PRIVACY_DPO_CONTACT:}
praxis.privacy.review-instructions=${PRAXIS_PRIVACY_REVIEW_INSTRUCTIONS:Solicite revisao pelo canal de atendimento configurado pela empresa responsavel pelo processo seletivo.}

praxis.object-storage.endpoint=${OBJECT_STORAGE_ENDPOINT:}
praxis.object-storage.public-url=${OBJECT_STORAGE_PUBLIC_URL:}
praxis.object-storage.region=${OBJECT_STORAGE_REGION:us-east-1}
praxis.object-storage.access-key=${OBJECT_STORAGE_ACCESS_KEY:}
praxis.object-storage.secret-key=${OBJECT_STORAGE_SECRET_KEY:}
praxis.object-storage.bucket=${OBJECT_STORAGE_BUCKET:praxis-media}
praxis.object-storage.path-style=${OBJECT_STORAGE_PATH_STYLE:true}

spring.servlet.multipart.max-file-size=${PRAXIS_MEDIA_MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${PRAXIS_MEDIA_MAX_REQUEST_SIZE:12MB}

# Timeouts (ms) de todas as chamadas HTTP de saída via RestClient (webhooks e Mercado Pago).
praxis.http.connect-timeout-ms=${PRAXIS_HTTP_CONNECT_TIMEOUT_MS:5000}
praxis.http.read-timeout-ms=${PRAXIS_HTTP_READ_TIMEOUT_MS:15000}

server.forward-headers-strategy=framework

springdoc.swagger-ui.path=/docs
springdoc.swagger-ui.enabled=${SPRINGDOC_SWAGGER_UI_ENABLED:true}
springdoc.api-docs.enabled=${SPRINGDOC_API_DOCS_ENABLED:true}
