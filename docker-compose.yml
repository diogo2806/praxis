services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: praxis-backend
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=praxis
      - DB_USER=${POSTGRES_USER:?POSTGRES_USER must be set}
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      - DB_SCHEMA=public
      - PRAXIS_JPA_DDL_AUTO=validate
      - PRAXIS_INTEGRATION_TOKEN=${PRAXIS_INTEGRATION_TOKEN:?PRAXIS_INTEGRATION_TOKEN must be set}
      - PRAXIS_JWT_SECRET=${PRAXIS_JWT_SECRET:?PRAXIS_JWT_SECRET must be set}
      - PRAXIS_PUBLIC_BASE_URL=${PRAXIS_PUBLIC_BASE_URL:-http://localhost}
      - SPRING_PROFILES_ACTIVE=prod
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - praxis-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: praxis-frontend
    ports:
      - "80:80"
    environment:
      - PRAXIS_API_BASE_URL=http://backend:8080
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - praxis-network

  postgres:
    image: postgres:17-alpine
    container_name: praxis-postgres
    environment:
      - POSTGRES_DB=praxis
      - POSTGRES_USER=${POSTGRES_USER:?POSTGRES_USER must be set}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d praxis"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    networks:
      - praxis-network

volumes:
  postgres_data:

networks:
  praxis-network:
    driver: bridge
